//#region src/index.ts
/** Host loader entry for the browser-only pet plugin. */
/** Provides no host-side behavior (the pet is a pure client plugin). */
function apply() {}
//#endregion
export { apply };
